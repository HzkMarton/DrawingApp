<UserControl x:Class="DrawingApp.PauseMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <UserControl.Resources>
            <!-- Same MenuButtonStyle as MainMenuView -->
            <Style x:Key="MenuButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="0,0,0,12"/>
                <Setter Property="Height" Value="44"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="#1C2135"/>
                <Setter Property="BorderBrush" Value="#1C2135"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </UserControl.Resources>

        <Border Margin="20" Background="White" BorderBrush="#1C2135"
            BorderThickness="2" CornerRadius="30">
            <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="Szünet" FontSize="28" FontWeight="SemiBold"
                       Foreground="#1C2135" HorizontalAlignment="Center"/>

                <!-- Buttons -->
                <UniformGrid Grid.Row="2" Rows="5" Columns="1">
                    <Button Style="{StaticResource MenuButtonStyle}" Content="Folytatás" Click="Folytatas_Click"/>
                    <Button Style="{StaticResource MenuButtonStyle}" Content="Mentés" Click="Mentes_Click"/>
                    <Button Style="{StaticResource MenuButtonStyle}" Content="Betöltés" Click="Betoltes_Click"/>
                    <Button Style="{StaticResource MenuButtonStyle}" Content="Beállítások" Click="Beallitasok_Click"/>
                    <Button Style="{StaticResource MenuButtonStyle}" Content="Főmenü" Background="#991B1B" BorderBrush="#991B1B" Click="Fomenu_Click"/>
                </UniformGrid>
            </Grid>
        </Border>
    </UserControl>